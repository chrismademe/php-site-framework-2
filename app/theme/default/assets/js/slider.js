$(document).ready(function() {

    // Init slick
    $('.slider').slick({
        autoplay: true,
        autoplaySpeed: 5000,
        arrows: false,
        fade: true,
        speed: 1000,
        slide: '.slide'
    });

});
